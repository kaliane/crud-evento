class Evento {
    constructor(id, nome, dataInicial, dataFinal, ativo, instituicao) {
        this.id = id;
        this.nome = nome;
        this.dataInicial = dataInicial;
        this.dataFinal = dataFinal;
        this.ativo = ativo;
        this.instituicao = instituicao;
    }
}

export default Evento;